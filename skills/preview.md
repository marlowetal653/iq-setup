Run the app in development mode, open it in the browser, and provide preview links.

Steps:
1. Read `package.json` to find the dev/start script
2. Run the dev server in the background
3. Wait for the server to be ready and extract both the local URL and the network URL
4. Open the local URL in the browser automatically: `open http://localhost:XXXX`
5. Give me both clickable preview links (local and network)
